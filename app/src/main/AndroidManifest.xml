<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cinggl.cinggl">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera2"
        android:required="false" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Cinggl"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".profile.FollowersActivity" />
        <activity android:name=".profile.FollowingActivity" />
        <activity android:name=".profile.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".profile.SignUpActivity" />
        <activity android:name=".camera.CreateCingleActivity" />
        <activity
            android:name=".home.HomeActivity"
            android:label="Home"
            android:theme="@style/AppTheme" />
        <activity android:name=".camera.CustomGelleryActivity" />
        <activity
            android:name=".ui.SettingsActivity"
            android:label="Settings" />
        <activity android:name=".profile.UpdateProfileActivity" />
        <activity android:name=".home.CommentsActivity"></activity>
    </application>

</manifest>